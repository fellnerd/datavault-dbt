# Anthropic API Key for Claude
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-xxxxx

# Optional: Set model (default: claude-sonnet-4-20250514)
# CLAUDE_MODEL=claude-sonnet-4-20250514

# ============== MCP Server Configuration ==============
# Port for the MCP HTTP server
MCP_PORT=3001
# Host to bind to (0.0.0.0 for all interfaces, 127.0.0.1 for local only)
MCP_HOST=0.0.0.0

# Multi-User Authentication Tokens
# Format: name:token,name2:token2
# Generate secure tokens with: openssl rand -hex 32
MCP_USER_TOKENS=alice:your-secure-token-here,bob:another-secure-token

# ============== Ollama Configuration ==============
# Ollama server URL
OLLAMA_HOST=http://localhost:11434
# Embedding model (nomic-embed-text has 768 dimensions)
OLLAMA_EMBED_MODEL=nomic-embed-text

# ============== SQLite Database ==============
# Path to SQLite database file (relative to agent directory)
SQLITE_PATH=./data/agent.db

# ============== RAG Configuration ==============
# Size of text chunks in characters
RAG_CHUNK_SIZE=500
# Overlap between chunks in characters
RAG_CHUNK_OVERLAP=50
# Number of similar chunks to retrieve
RAG_TOP_K=5
# Minimum similarity threshold (0.0 to 1.0)
RAG_SIMILARITY_THRESHOLD=0.5
